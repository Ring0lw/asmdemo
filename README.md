![demo](.github/assets/demo.gif)

# 3d Demo written in AArch64 macOS assembly

## Compile:

```bash
make
```

## Run:

```bash
./demo
```

Ctrl+C to exit, `make run` is the same thing with an fps counter


I recommend ghostty or alacritty. default macoOS terminal lags

